import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foodtime.app',
  appName: 'Food Time',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
